<!DOCTYPE html>
<html lang="en">
	<%- include("../partials/head.ejs") %>

	<body>
		<%- include("../partials/header.ejs") %>
		<main>
			<div>
				<article>
					<h2>
						<%= sneaker.name %> - <%= sneaker.deadstock ? "Yes" : "No" %>
					</h2>
					<h3><%= sneaker.img %></h3>
					<a href="/sneakers/<%= sneaker._id %>/edit"><button>Edit</button></a>
					<form action="/sneakers/<%= sneaker._id %>?_method=DELETE" method="POST">
						<input type="submit" value="Delete" />
					</form>
					<a href="/sneakers/"><button>Back to Main</button></a>
				</article>
			</div>
		</main>
	</body>
</html>
